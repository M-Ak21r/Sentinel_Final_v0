# Sentinel Unified - Python Dependencies
# ========================================
# Core Vision & AI
ultralytics>=8.0.0
insightface>=0.7.3
# CUDA 12.x compatible versions
onnxruntime-gpu==1.19.2
opencv-python>=4.8.0
numpy>=1.24.0

# PyTorch with CUDA support (install separately with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121)
# torch (CUDA 12.1 compatible)
# torchvision (CUDA 12.1 compatible)

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0

# IoT & Messaging
paho-mqtt>=1.6.0

# Configuration & Environment
python-dotenv>=1.0.0

# Utilities
requests>=2.31.0
